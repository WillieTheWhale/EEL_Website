# E.E.L. Backend Configuration

# Server Port (OpenShift uses 8080 by default)
PORT=8080

# Admin Dashboard Password
ADMIN_PASSWORD=change-me-to-a-secure-password

# Email Configuration (for application notifications)
#
# Option A — UNC relay (no authentication, works on CloudApps):
#   SMTP_HOST=relay.unc.edu
#   SMTP_PORT=587
#   SMTP_FROM=wilk05@unc.edu
#   (leave EMAIL_USER and EMAIL_PASS unset)
#
# Option B — Gmail SMTP (local dev). Use an App Password:
#   https://myaccount.google.com/apppasswords
#   EMAIL_USER=your-email@gmail.com
#   EMAIL_PASS=your-app-password
#
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-app-password

# Notification recipient (receives applications with resume attached)
NOTIFY_EMAIL=wilk05@unc.edu

# Optional: Generic SMTP (overrides Gmail default)
# SMTP_HOST=relay.unc.edu
# SMTP_PORT=587

# Sender address (defaults to EMAIL_USER; required when using unauthenticated relay)
# SMTP_FROM=wilk05@unc.edu

# Site URL (used for CORS origin restriction and email links)
# In production, set to your actual domain (e.g., https://eel-website.apps.your-cluster.openshiftapps.com)
SITE_URL=http://localhost:8080
